---
import BaseLayout from './BaseLayout.astro';
import { site } from '../data/siteData.js';
const { parent, childServices, title, description, canonicalPath } = Astro.props;
---
<BaseLayout title={title} description={description} canonicalPath={canonicalPath}>
  <section class="hero compact">
    <img src="/images/hero.png" alt={site.business} />
    <div class="hero-copy"><p class="eyebrow">{site.category} in Frederick</p><h1>{parent.name} in Frederick, MD</h1><p>{parent.shortDescription}</p><a href="/contact/" class="btn">Plan My Project</a></div>
  </section>
  <section class="section split">
    <div><h2>Local {parent.name} planning</h2><p>{parent.overview[1]}</p><p>{parent.overview[2]}</p><p>{parent.overview[3]}</p></div>
    <aside class="panel"><h3>What We Check</h3>{parent.keyPoints.map((point) => <p>{point}</p>)}<p>Fence line, gates, slope, access, materials, utilities, and HOA or neighbor constraints.</p></aside>
  </section>
  <section class="section"><h2>{parent.name} services</h2><div class="cards">{childServices.map((child) => <a class="card" href={`/services/${parent.slug}/${child.slug}/`}><h3>{child.name}</h3><p>{child.summary}</p></a>)}</div></section>
  <section class="cta"><h2>Ready for a Frederick-specific fence plan?</h2><p>Tell us what the fence needs to solve and we will help map layout, materials, timing, access, and the next right step.</p><a href="/contact/" class="btn light">Request Service</a></section>
</BaseLayout>
